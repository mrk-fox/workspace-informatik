# WeatherVector Documentation

## Classes

### Vector

```java
public Vector(Temperature p_temp, Humidity p_hum, WindSpeed p_windv, WindDir p_windd, Precipitation p_precip, boolean p_delta)
```

Full getters with String output.
v is a any Object of the Vector class!

```java
v.getTemp().getTemp() //Temperature
v.getHum().getHum() //Humidity
v.getWindv().getSpeed() //Wind Speed
v.getWindd().getWDir() //Wind direction
v.getPrecip().getPrecip() //Precipitation total
v.getIsDelta() //Is this Vector a delta vector?
```



